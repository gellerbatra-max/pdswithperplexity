// TODO(step-6): assemble the shell — top bar, piece tray, canvas, right dock,
// bottom ribbon, status bar. Step 1 is scaffold only: this renders nothing.
export const App = () => <div id="marker-root" />;
