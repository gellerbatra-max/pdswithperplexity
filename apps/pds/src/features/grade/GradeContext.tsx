import { PanelSection } from '@/components/PanelSection';

export const GradeContext = () => (
  <PanelSection title="Sizes" caption="0">
    <p className="muted">
      No size chart yet. A chart defines the base size and the increments every grade
      rule is applied against.
    </p>
  </PanelSection>
);
