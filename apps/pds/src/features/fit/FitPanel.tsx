import { PanelSection } from '@/components/PanelSection';

export const FitPanel = () => (
  <PanelSection title="Ease">
    <p className="muted">
      Ease compares finished pattern measurements against body measurements per size.
    </p>
  </PanelSection>
);
