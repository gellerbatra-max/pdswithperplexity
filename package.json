{
  "name": "pdswithperplexity",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/pds",
    "build": "npm run build --workspace apps/pds",
    "preview": "npm run preview --workspace apps/pds",
    "typecheck": "npm run typecheck --workspace apps/pds",
    "check:offset": "npm run check:offset --workspace apps/pds",
    "check:curve": "npm run check:curve --workspace apps/pds",
    "check": "npm run check --workspace apps/pds",
    "check:roundtrip": "npm run check:roundtrip --workspace apps/pds",
    "check:grading": "npm run check:grading --workspace apps/pds",
    "check:dxf": "npm run check:dxf --workspace apps/pds",
    "check:rul": "npm run check:rul --workspace apps/pds",
    "report:dxf": "npm run report:dxf --workspace apps/pds"
  }
}
